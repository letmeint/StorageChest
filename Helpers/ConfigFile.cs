using BepInEx.Configuration;

namespace StorageChest.Helpers;

internal static class ConfigFile
{

    //internal static ConfigEntry<bool> VRHeadSetSwapEnabled;
    //internal static ConfigEntry<string> spriteFolderPath;
    //internal static ConfigEntry<string> VRHeadSetDataPath;

    //internal static void Init(BepInEx.Configuration.ConfigFile config)
    //{
    //    VRHeadSetSwapEnabled = config.Bind("General", "Enabled", true, "Enable the plugin");
    //    spriteFolderPath = config.Bind<string>("General", "SpriteFolderPath", Globals.defaultSpriteFolderPath, "Path to the folder containing the sprites");
    //    VRHeadSetDataPath = config.Bind<string>("General", "VRHeadSetDataPath", Globals.defaultSpriteDataPath, "Path to the folder containing the sprite data for VRHeadset");
    //}
}
